# build-tools uses no environment variables — the tools are self-contained
# scripts that read their inputs from argv and the filesystem. This file
# exists so `make setup`'s copy line stays harmless and the repo documents
# that absence explicitly.
